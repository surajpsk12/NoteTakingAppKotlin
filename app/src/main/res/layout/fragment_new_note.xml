<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/keep_background"
        android:fitsSystemWindows="true"
        tools:context=".fragments.NewNoteFragment">

        <!-- App Bar Layout for Google Keep Header -->
        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/keep_background"
            app:elevation="0dp">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/keep_background"
                app:navigationIcon="@drawable/ic_arrow_back"
                app:navigationIconTint="@color/keep_text_primary"
                app:titleTextColor="@color/keep_text_primary" />

        </com.google.android.material.appbar.AppBarLayout>

        <!-- Main Content Area -->
        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingStart="16dp"
                android:paddingTop="16dp"
                android:paddingEnd="16dp"
                android:paddingBottom="16dp">

                <!-- Note Content Card -->
                <androidx.cardview.widget.CardView
                    android:id="@+id/cardView2"
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_marginBottom="16dp"
                    android:background="@color/keep_surface"
                    app:cardBackgroundColor="@color/keep_surface"
                    app:cardCornerRadius="12dp"
                    app:cardElevation="2dp"
                    app:cardMaxElevation="4dp"
                    app:cardUseCompatPadding="false"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:orientation="vertical"
                        android:padding="20dp">

                        <!-- Title Input -->
                        <EditText
                            android:id="@+id/etNoteTitle"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:minHeight="48dp"
                            android:background="@android:color/transparent"
                            android:fontFamily="sans-serif-medium"
                            android:hint="Title"
                            android:inputType="text|textCapSentences"
                            android:maxLines="3"
                            android:paddingStart="0dp"
                            android:paddingTop="12dp"
                            android:paddingEnd="0dp"
                            android:paddingBottom="12dp"
                            android:textColor="@color/keep_text_primary"
                            android:textColorHint="@color/keep_text_hint"
                            android:textSize="18sp"
                            android:textStyle="normal" />

                        <!-- Divider -->
                        <View
                            android:layout_width="match_parent"
                            android:layout_height="1dp"
                            android:layout_marginBottom="16dp"
                            android:alpha="0.12"
                            android:background="@color/keep_text_primary" />

                        <!-- Body Input -->
                        <EditText
                            android:id="@+id/etNoteBody"
                            android:layout_width="match_parent"
                            android:layout_height="0dp"
                            android:layout_weight="1"
                            android:background="@android:color/transparent"
                            android:fontFamily="sans-serif"
                            android:gravity="top|start"
                            android:hint="Note"
                            android:inputType="textMultiLine|textCapSentences"
                            android:lineSpacingExtra="4dp"
                            android:minHeight="200dp"
                            android:paddingStart="0dp"
                            android:paddingTop="0dp"
                            android:paddingEnd="0dp"
                            android:paddingBottom="8dp"
                            android:scrollbars="vertical"
                            android:textColor="@color/keep_text_secondary"
                            android:textColorHint="@color/keep_text_hint"
                            android:textSize="16sp" />

                    </LinearLayout>

                </androidx.cardview.widget.CardView>

            </androidx.constraintlayout.widget.ConstraintLayout>

        </androidx.core.widget.NestedScrollView>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>